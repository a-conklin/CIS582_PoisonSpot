Hagrid data is super slow to transform, so cached values are stored here to try and speed up some processes, or serve as a checkpoint if a process fails late.
Generally this happens on the first run. Cached data can be multiple gigabytes so I wasn't able to upload any pregenerated data here. You'll have to just do a run to get some.
